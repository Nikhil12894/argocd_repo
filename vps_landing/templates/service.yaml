apiVersion: v1
kind: Service
metadata:
  name: {{ include "vps_landing.fullname" . }}
  labels:
    {{- include "vps_landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vps_landing.selectorLabels" . | nindent 4 }}
