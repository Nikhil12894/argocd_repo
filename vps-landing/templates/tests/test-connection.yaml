apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vps_landing.fullname" . }}-test-connection"
  labels:
    {{- include "vps_landing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vps_landing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
